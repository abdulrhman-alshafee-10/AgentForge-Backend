import { Router } from 'express';

// ─── Streaming router ─────────────────────────────────────────────────────────
// Phase 01: placeholder only.
// Phase 05 implements the SSE stream endpoint.

const router = Router();

export { router as streamingRouter };
