import { Router } from 'express';

// ─── Messages router ──────────────────────────────────────────────────────────
// Phase 01: placeholder only.
// Phase 04 implements POST + GET.

const router = Router();

export { router as messagesRouter };
