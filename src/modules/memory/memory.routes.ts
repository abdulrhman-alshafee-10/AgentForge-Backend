import { Router } from 'express';

// ─── Memory router ────────────────────────────────────────────────────────────
// Phase 01: placeholder only.
// Phase 12 implements CRUD for user memories.

const router = Router();

export { router as memoryRouter };
