import { Router } from 'express';

// ─── Workflows router ─────────────────────────────────────────────────────────
// Phase 01: placeholder only.
// Phase 08 adds LangGraph workflow definitions and management.

const router = Router();

export { router as workflowsRouter };
