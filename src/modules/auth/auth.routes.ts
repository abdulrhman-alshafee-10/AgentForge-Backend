import { Router } from 'express';

// ─── Auth router ──────────────────────────────────────────────────────────────
// Phase 01: placeholder only.
// Phase 03 implements: register, login, refresh, logout, me.

const router = Router();

export { router as authRouter };
