import { Router } from 'express';

// ─── Tools router ─────────────────────────────────────────────────────────────
// Phase 01: placeholder only.
// Phase 07 implements the tool registry and execution endpoints.

const router = Router();

export { router as toolsRouter };
