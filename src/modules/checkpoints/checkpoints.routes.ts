import { Router } from 'express';

// ─── Checkpoints router ───────────────────────────────────────────────────────
// Phase 01: placeholder only.
// Phase 10 implements checkpoint persistence and recovery endpoints.

const router = Router();

export { router as checkpointsRouter };
