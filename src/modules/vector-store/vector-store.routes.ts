import { Router } from 'express';

// ─── Vector-store router ──────────────────────────────────────────────────────
// Phase 01: placeholder only.
// Phase 06 implements the pgvector search endpoints.

const router = Router();

export { router as vectorStoreRouter };
